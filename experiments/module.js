// dynamically generated -> can use a custom DiagnosticsAdapter to analyze models on changes (works transitively)
// javascript text model for the editor -> defines the type of the variable(one for each module)
__moduleDefinitions["asdf"] = defineModule("asdf", () => {
	return class Foo {
		a = 1;
		b() {
			return "";
		}
	}
});