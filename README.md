Playground for the [Monaco editor](https://github.com/Microsoft/monaco-editor) using ASP.NET Core and TypeScript.

## Running
```sh
npm install
npm run build
dotnet run
```
Alternatively to the build commands you can use `Cmd-Shift-B` followed by `F5` inside VS Code.
