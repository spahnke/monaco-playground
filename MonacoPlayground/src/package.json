{
  "name": "monaco-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "prebuild": "rm -rf ../wwwroot/dist",
    "build": "tsc",
    "postbuild": "rsync -R ./languages/**/*.json ../wwwroot/dist",
    "watch": "npm run build && tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.8.0-dev.20191026"
  }
}
